model_params:
  max_depth: 5
  learning_rate: 0.1
  subsample: 0.9
  colsample_bytree: 0.8
  gamma: 0.1
  n_estimators: 100
  scale_pos_weight: 578  # For imbalanced classes

data_params:
  test_size: 0.2
  random_state: 42

prediction:
  threshold: 0.5